{
  "name": "the_acme_store",
  "version": "1.0.0",
  "description": "A simple Express.js web application for the Acme Store",
  "main": "index.js",
  "scripts": {
    "start": "node server/index.js",
    "dev": "nodemon server/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.17.1",
    "pg": "^8.5.1",
    "bcrypt": "^5.0.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.7"
  }
}
